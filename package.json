{
  "name": "music-player",
  "version": "1.0.0",
  "description": "revamp using hybrid app approach.",
  "main": "index.js",
  "scripts": {
    "start": "serve-lite www",
    "test": "tsc --noEmit",
    "build": "esbuild src/app.ts --bundle --outfile=www/js/bundle.js",
    "android": "npm run build && cap copy android",
    "type-check": "tsc -p . --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor/core": "^5.6.0",
    "@capacitor/filesystem": "^5.2.0"
  },
  "devDependencies": {
    "@capacitor/android": "^5.6.0",
    "@capacitor/cli": "^5.6.0",
    "@types/node": "^20.11.4",
    "esbuild": "^0.19.11",
    "serve-lite": "^1.2.1",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}
